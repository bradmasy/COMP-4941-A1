@model IEnumerable<assignment.Models.Job>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Customer.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Service.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Customer.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Service.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { serviceId = item.ServiceId, customerId = item.CustomerId }) |
            @Html.ActionLink("Details", "Details", new { serviceId = item.ServiceId, customerId = item.CustomerId }) |
            @Html.ActionLink("Delete", "Delete", new { serviceId = item.ServiceId, customerId = item.CustomerId })
        </td>
    </tr>
}

</table>
